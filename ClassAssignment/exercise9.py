# Define a string containing all the required characters
# Loop through each character and display its integer equivalent

characters = "BCDbcd012$*+ "

for char in characters:
    print(f"The integer value of '{char}' is {ord(char)}")

